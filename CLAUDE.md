# ملاحظات الصيانة — المكتبة الجامعة (النسخة المنشورة)

هذا الملف بيوثّق التعديلات اللي اتعملت على النسخة المنشورة فعليًا (islamiclibraries.com)
بتاريخ 13 يوليو 2026.

## 1) إصلاح روابط islamqa.info القديمة (6 مواضع)

الروابط كانت بصيغة قديمة `islamqa.info/ar/answers/categories/N` مش موجودة في هيكل الموقع
الحالي (اتأكد إنها فعلاً بترجع نتيجة فاشلة، بينما الصيغة الصحيحة الحالية هي
`islamqa.info/ar/categories/topics/N`). بما إن رقم التصنيف (N) في النظام القديم مش بالضرورة
نفسه في النظام الجديد، تم استبدال كل الروابط الستة بشجرة التصنيفات العامة
`https://islamqa.info/ar/categories/topics` (اتأكد إنها شغالة) بدل تخمين رقم تصنيف غلط.

**الملفات المتأثرة:** `fatawa/adab.html`، `fatawa/ibadat.html`، `fatawa/usra.html`،
`fatawa/muamalat.html` (مرتين)، `apps.html`.

⚠️ **ملحوظة مهمة:** رابط قناة يوتيوب "اللجنة الدائمة" في `videos-fiqh.html`
(`youtube.com/channel/UCMA_3yMgxZKgNteEvXtkxmA`) **اتفحص واتأكد إنه صحيح وشغال بالفعل** —
ملوش داعي لأي تعديل، وده مختلف عن نسخة تانية من الموقع كانت باستخدام رابط `@alifta` غير
مؤكد. لو حد يوم اقترح تغييره، لازم يتفحص الأول.

## 2) مصدر احتياطي لصفحة القرآن الكريم (quran.html)

**المشكلة:** `api.alquran.cloud` (المصدر الأساسي لنص وتفسير وصوت القرآن) بيتعرض لهجمات DDoS
متكررة من سبتمبر 2025 مستمرة لحد الآن (يوليو 2026)، وفريقه اضطر يحظر مئات الآلاف من عناوين
IP كإجراء دفاعي — بينها نطاقات واسعة في مصر والمغرب وباكستان وبنجلاديش وإندونيسيا (مؤكد من
نقاش رسمي على `community.islamic.network/d/167`). النتيجة: زوار كتير بيشوفوا الصفحة عالقة
على "جارِ تحميل قائمة السور..." للأبد، رغم إن كود الصفحة سليم 100%.

**الحل المطبَّق في `assets/js/quran.js`:**
- `fetchWithRetry()`: إعادة محاولة تلقائية واحدة بعد 1.2 ثانية (لمعالجة الانقطاعات المؤقتة).
- مصدر احتياطي كامل: `quranapi.pages.dev` (مستضاف على Cloudflare Pages، بنية تحتية مختلفة
  تمامًا عن `api.alquran.cloud`، فمش متأثر بنفس الحظر). بيتفعّل تلقائيًا عند فشل المصدر
  الأساسي لثلاث وظائف: تحميل قائمة السور، فتح نص السورة، والاستماع للسورة كاملة (عبر رابط
  صوتي بديل من GitHub).
- **قيود المصدر الاحتياطي:** لا يوجد فيه تفسير ميسّر (الزر بيختفي تلقائيًا بدل عرض بيانات
  فارغة)، ولا استماع لآية منفردة (يحتاج رقم آية عام غير متوفر في المصدر البديل). البحث
  (`searchQuran`) لسه بيعتمد على المصدر الأساسي فقط.
- شريط تنبيه شفاف (`fallbackNotice()`) بيظهر للمستخدم لما الصفحة تشتغل بالمصدر الاحتياطي.

**اختبار مطلوب:** الكود اتفحص بـ `node --check` فقط؛ يُفضّل اختبار حي في متصفح حقيقي بعد النشر
(مثلاً بحجب `api.alquran.cloud` مؤقتًا في أدوات المطوّر والتأكد من ظهور شريط الـ fallback).

## 3) حاسبة المواريث (الفرائض) — ميزة جديدة في tools.html

محرك حساب كامل في `assets/js/faraid.js` (كسور مضبوطة تمامًا، بدون أي floating point)،
مدموج كبطاقة أداة جديدة في `tools.html` تحت حاسبة الزكاة مباشرة.

**الورثة المدعومون:** الزوج/الزوجة (تعدد زوجات مدعوم)، الأب، الأم، الجد لأب، الجدة،
الأبناء، البنات، الإخوة والأخوات الأشقاء فقط.

**الأحكام المطبَّقة:** الفروض الثابتة، الحجب (الجد بالأب، الجدة بالأم، الإخوة بالأب/الجد/الابن)،
التعصيب (الابن مع البنات، الأب/الجد عند غياب الابن، الإخوة مع الأخوات، الأخوات مع البنات
عند غياب الإخوة)، العول (تلقائي)، الرد (لغير الزوجين فقط)، وحالتا الغراوين/العُمَريّتان
(ثلث الباقي بعد الزوج لا ثلث كل التركة — برأي الجمهور).

**التحقق:** تم اختبار المحرك بـ 11 حالة فقهية معروفة (المنبرية، العُمَريّتان، العول، الرد،
حالات الأبناء والإخوة المختلطة) وطابقت كل النتائج المراجع الفقهية الكلاسيكية تمامًا. تم
اكتشاف وإصلاح باج حقيقي أثناء الاختبار في توزيع باقي العصبة على أكثر من وارث من نفس الفئة.

**غير مدعوم (موثّق في تحذير الحاسبة نفسها):** أبناء الابن، الإخوة لأب أو لأم فقط، الأعمام،
ذوو الأرحام، الوصية والدين بالتفصيل. أي حالة فيها هذه العناصر لازم تُعرض على عالم شرعي.

## ⚠️ ملاحظة عن فجوة المحتوى

وقت العمل على هذه الإصلاحات، لوحظ إن نسخة أخرى من الموقع (مش المنشورة) فيها محتوى أكبر
بكتير: 105 قارئ (مقابل 63 في المنشورة) و500+ كتاب (مقابل 175 في المنشورة). الفجوة دي
موجودة برضه في: `assets/js/fatawa-data.js`، `assets/js/books-data.js`،
`assets/js/glossary-data.js`، `assets/js/reciters-data.js`، `assets/js/topics.js`.
لم يتم لمس هذا المحتوى ضمن هذا الإصلاح لعدم معرفة مصدر الفجوة أو أيّ نسخة هي الأحدث فعليًا —
يحتاج صاحب الموقع يراجع ده بنفسه ويقرر أي نسخة يعتمدها.

## ✅ تحديث (13 يوليو 2026): دمج القراء داخل صفحة قراءة القرآن + إزالة صوت العفاسي الوحيد

بناءً على طلب صاحب الموقع، تم دمج نظام القراء (اللي كان منفصل في `quran-reciters.html`) داخل
تجربة قراءة السورة في `quran.html` نفسها، وإزالة الاعتماد على تلاوة "العفاسي" الوحيدة اللي
كانت مربوطة بـ `cdn.islamic.network` (نفس البنية التحتية المتأثرة بمشاكل الحظر الموثقة أعلاه).

**اللي اتغيّر في `assets/js/quran.js`:**
- شيلت `CDN_AYAH` و`CDN_SURAH` (كانتا بتستخدموا `cdn.islamic.network/.../ar.alafasy` فقط).
- زر "🔊 استماع للسورة كاملة (العفاسي)" استُبدل بـ `<select id="reciterSelect">` يعرض كل
  الـ63 قارئ من `window.RECITERS` (مصدرها `assets/js/reciters-data.js`، مبني على mp3quran.net
  — مصدر صوتي مستقل تمامًا عن Al Quran Cloud، فالتلاوة دلوقتي مش متأثرة بمشاكل حظر الـ DDoS
  الموثقة فوق).
- تفضيل القارئ بيتحفظ في `localStorage` (`quran_reciter_id`) عشان يفتكر اختيار المستخدم في
  زياراته الجاية.
- **الاستماع لآية منفردة اتشال بالكامل** (كان بيستخدم `cdn.islamic.network` بصوت العفاسي فقط،
  ومفيش بديل مكافئ عند mp3quran.net لأنه بيوفر ملفات بمستوى السورة كاملة مش الآية). لو حابب
  ترجّعها مستقبلًا، محتاج مصدر صوتي تاني بيدعم التقطيع لكل آية.
- ملحوظة الـ API العلوية في `quran.html` اتحدّثت توضّح إن النص/التفسير من مصدر، والصوت من
  مصدر تاني مستقل، عشان الشفافية مع الزائر.
- زرار "تصفّح كل القراء" في أعلى الصفحة اتسيب (بيودّي لـ `quran-reciters.html`) لأن الصفحة دي
  لسه مفيدة لمين عايز يدوّر بالاسم/التصنيف على قارئ معيّن من غير قراءة سورة بعينها — مش نفس
  استخدام اختيار القارئ جوه صفحة القراءة.

**اختبار مطلوب:** الكود اتفحص بـ `node --check` بس محتاج اختبار حي في متصفح حقيقي (فتح سورة،
اختيار قارئ من القائمة، تشغيل الصوت، والتأكد إن الرابط اللي بيتبني من `getSurahUrl()` شغال
فعليًا لعدد من القراء المختلفين).

## ✅ إصلاحات SEO (13 يوليو 2026)

**باج خطير اتصلح:** `sitemap.xml` (49 رابط) و`robots.txt` وJSON-LD في `index.html` كانوا
كلهم بيشيروا لدومين قديم خطأ `islamicplatform.replit.app` بدل الدومين الفعلي
`islamiclibraries.com`. ده معناه إن Google كان بيستقبل sitemap فيه روابط لدومين تاني تمامًا،
وده بيضعف الفهرسة بشكل كبير جدًا. تم استبدال كل الحالات بالدومين الصحيح.

**إضافة canonical + og:url لكل الصفحات (56 صفحة):** الموقع ملوش أي `<link rel="canonical">`
ولا `<meta property="og:url">` خالص قبل كده على أي صفحة. اتضافوا تلقائيًا لكل الـ56 صفحة،
كل واحدة بالرابط الصحيح بتاعها، عشان تمنع مشاكل المحتوى المكرر وتوضّح لجوجل النسخة
الرسمية من كل صفحة.

**تم فحصه ولوحظ إنه سليم بالفعل (مفيش داعي لتعديل):**
- كل الصور عندها `alt` نصي — صفر صور ناقصة alt.
- كل الصفحات عندها `meta name="description"` مخصّصة.

**لسه TODO (يحتاج قرار/حساب من صاحب الموقع، مش تعديل كود):**
- عمل حساب Google Search Console وBing Webmaster Tools وربط الدومين، ثم إعادة تقديم
  sitemap.xml الصحيح الجديد (مهم جدًا بعد إصلاح الدومين).
- عداد زيارات خاص (تحليلات) — شوف قسم الكاونتر في نفس المحادثة.

## ✅ تحديثات إضافية (13 يوليو 2026): عداد زيارات + إصلاحات PageSpeed

**عداد الزيارات (GoatCounter):** تم إضافة سكريبت GoatCounter الخاص بصاحب الموقع
(`princezizo.goatcounter.com`) قبل `</body>` في كل الـ56 صفحة. اللوحة متاحة على
`princezizo.goatcounter.com` (تسجيل دخول صاحب الموقع فقط، مش ظاهر للزوار).

**نتائج PageSpeed Insights (13 يوليو 2026) قبل الإصلاح:** Desktop 98 أداء / 92 إتاحة / 100
أفضل الممارسات / 100 SEO. Mobile 77 أداء / 92 إتاحة / 100 / 100. المشاكل المذكورة: طلبات
تعطّل التحميل (Render-blocking، أهمها خطوط Google)، تباين ألوان غير كافٍ، والمستند من غير
"main landmark".

**الإصلاحات المطبَّقة:**
1. **`<main>` landmark:** كل صفحة من الـ56 كانت من غير عنصر `<main>` (مباشرة من `</header>`
   لسلسلة `<section>` لحد `<footer>`). تم لف كل المحتوى بين الهيدر والفوتر بعنصر `<main>` في
   كل الصفحات — بيحل تحذير "Document does not have a main landmark".
2. **تحميل خطوط Google بدون تعطيل العرض:** كان `<link rel="stylesheet">` لخطوط Google
   Fonts (Amiri/Cairo/Tajawal) render-blocking بشكل كامل. تم تحويله لنمط التحميل غير
   المتزامن القياسي (`media="print" onload="this.media='all'"` + `<noscript>` fallback)، مع
   إضافة `preconnect` لـ `fonts.gstatic.com` (كان ناقص، بس `fonts.googleapis.com` كان موجود).
   هذا التغيير طُبّق على كل الـ56 صفحة.
3. **تباين الألوان (`--gold` على خلفية بيضاء):** اللون الذهبي `#B8894A` المستخدم كنص
   (eyebrow، tags، تصنيفات) على خلفية بيضاء كانت نسبة تباينه 3.13:1 فقط — أقل من حد WCAG AA
   (4.5:1 للنص العادي). تم إضافة متغيّر جديد `--gold-text:#8A6230` (نسبة تباين 5.4:1) واستخدامه
   في الـ8 مواضع اللي بتستخدم الذهبي كلون نص فقط، بينما اللون الأصلي `--gold` سيبته زي ما هو
   في استخداماته كخلفية (badges/أزرار) لأنه بيتباين صح مع النص الغامق فوقه ولو اتغيّر كان
   هيبوّظ التباين هناك بدل ما يصلحه. في الوضع الداكن، `--gold-text` بيساوي `--gold` لأن
   تباينه أصلًا ممتاز (7.26:1).

**لسه TODO (مش أولوية، لكن يستاهل مراجعة لاحقًا):**
- "Render-blocking requests" و"Use efficient cache lifetimes" في تقرير PageSpeed لسه فيهم
  جزء متعلق بموارد خارجية (خطوط Google نفسها، ليست من طرفنا) — مش تحت تحكم كامل من الموقع.
- إعادة تشغيل PageSpeed test بعد النشر للتأكد من تحسّن نتيجة Mobile (كانت 77) والـ
  Accessibility (كانت 92) فعليًا.

## ⚠️ تحديث مهم (13 يوليو 2026): فحص روابط الكتب — معدل خطأ مقلق

بناءً على طلب صاحب الموقع "راجع روابط الكتب زي ما عملت مع القرآن"، تم فحص عينة من 175 كتاب
في `assets/js/books-data.js` (كل كتاب مربوط برقم شامله `shamela:N`، الرابط النهائي
`https://shamela.ws/book/N`).

**الفرق عن مشكلة القرآن:** shamela.ws نفسه سليم وموثوق (اتأكد: دومين عمره 20 سنة، خلف
Cloudflare، بدون بلاغات حظر إقليمي أو DDoS زي api.alquran.cloud). المشكلة هنا مختلفة تمامًا:
**أرقام كتب غلط في البيانات نفسها** — يعني الرابط "شغال" لكنه بيودّي لكتاب تاني خالص.

**النتيجة:** من عينة 13 كتاب اتفحصوا يدويًا (بحث ويب لكل رقم كتاب والتأكد من مطابقة العنوان)،
**4 كانوا غلط (~31%)**:
| id في البيانات | العنوان | الرقم الغلط | الرقم الصحيح |
|---|---|---|---|
| 162 | شرح لمعة الاعتقاد (ابن عثيمين) | 30855 | 11366 |
| 111 | الدعاء للمحاملي | 717 (كتاب تاني تمامًا: المئوية في إعراب القرآن) | 8255 |
| 24 | مرعاة المفاتيح شرح المصابيح | 244 | 8862 |
| 32 | الشرح الكبير على المقنع | 320 | 19188 |

الأربعة اتصلحوا في `assets/js/books-data.js`. باقي الـ175 (خصوصًا التسعة كتب المتبقية من
عينة الـ13، وكل الكتب اللي معملهاش فحص) **لسه محتاجة مراجعة** — معدل الخطأ ده مقلق كفاية إن
مراجعة شاملة لكل الـ175 تستاهل وقت مخصص (فحص كل كتاب لوحده مكلف جدًا من ناحية عدد
عمليات البحث المطلوبة، توقّف الفحص هنا لأسباب عملية).

**ملاحظة إضافية:** لوحظ وجود مصدر بيانات تاني منفصل تمامًا فيه روابط شاملة "verified: true"
في `assets/js/topics.js` (يُستخدم في قسم "أسئلة شائعة" على الصفحة الرئيسية) — لم يتم فحصه
في هذه الجلسة، ولا يوجد ما يؤكد أن علامة "verified" فيه دقيقة فعلًا بما إن نفس الشيء كان
مكتوب ضمنيًا عن كتب books-data.js.

**الحل الجذري المطبَّق (بدل انتظار فحص كل رقم):** في `books-all.html`، كل بطاقة كتاب دلوقتي
فيها **رابط احتياطي ثانوي**: "🔍 لم يفتح الكتاب الصحيح؟ دوّر عليه بالاسم" — بيودّي لبحث Google
مقيّد بـ `site:shamela.ws` باسم الكتاب والمؤلف. كده حتى لو رقم معيّن غلط ولسه محتاج تصحيح،
الزائر مش هيوصل لصفحة غلط بدون طريقة يلاقي بيها الكتاب الصحيح.

**التوصية:** لو حابب نكمل فحص باقي الـ~160 كتاب، ده شغل منفصل يستاهل جلسة مخصصة (يحتاج
بحث ويب لكل كتاب على حدة للتأكد من رقمه، وده مكلف بس ضروري بما إن معدل الخطأ الحالي عالي).

## ✅ إصلاحات إضافية (13 يوليو 2026)

- **`apps.html`**: كانت القائمة العلوية (desktop nav) ناقصة رابطي "أدوات" و"القاموس" رغم
  وجودهم في قائمة الموبايل والفوتر لنفس الصفحة — تم إضافتهم. اتفحصت باقي الـ55 صفحة ولقيتها
  سليمة (كل الروابط الأربعة: أدوات/قاموس موجودين في كل صفحة).
- **دمج صفحة القراء في صفحة القرآن:** بناءً على طلب صاحب الموقع "صفحة واحدة مش صفحتين"،
  تم دمج `quran-reciters.html` بالكامل داخل `quran.html` كتبويب ثانٍ ("🎙️ تصفح القراء") بجانب
  تبويب "📖 قراءة السور". `quran-reciters.html` بقت صفحة تحويل تلقائي (meta refresh + canonical)
  لـ `quran.html#reciters` عشان أي رابط قديم أو نتيجة بحث مفهرسة تفضل شغالة. تم حذف رابط
  "قراء القرآن" المستقل من القائمة العلوية وقائمة الموبايل والفوتر في كل الـ18 صفحة اللي كانت
  بتحتوي عليه (كان بيروح لصفحة اتلغت).
- **تباين ألوان إضافي:** لوحظ إن الإصلاح السابق لـ `--gold` (13 يوليو) كان في `assets/css/style.css`
  المشترك بس، ومعملش على 3 استخدامات `color:var(--gold)` كانت جوه `<style>` مكتوبة داخل
  صفحات بعينها (`seerah/timeline.html`، `fatawa.html`، `quran.html`) — تم تصحيحهم لاستخدام
  `--gold-text` بردو.

## ✅ إزالة حرف "ق" من الشعار (13 يوليو 2026)

بناءً على طلب صاحب الموقع، تم حذف الحرف الزخرفي "ق" (كان في مربع صغير جنب اسم "المكتبة
الجامعة" في الهيدر والفوتر بكل صفحة) — الشعار دلوقتي نص فقط: "المكتبة الجامعة". تم:
- إزالة `<span class="mark">ق</span>` من كل الصفحات (110 مكان في 55 صفحة، الهيدر والفوتر
  في كل صفحة).
- حذف تنسيق `.logo .mark` من `assets/css/style.css` بما إنه بقى غير مستخدم خالص.
- `.logo` كان أصلًا `display:flex` فمفيش أي تعديل تاني مطلوب على التخطيط — النص بيظهر لوحده
  بشكل طبيعي.

## إعلانات Google AdSense (1 أغسطس 2026)

كود AdSense مضاف في `<head>` كل صفحة HTML في الموقع (الـ56 صفحة الموجودة وقت الإضافة):

```html
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7919896989773628" crossorigin="anonymous"></script>
```

**⚠️ مهم: أي صفحة HTML جديدة تُضاف للموقع مستقبلًا لازم يتضاف فيها نفس السطر ده جوه `<head>`.**
استخدم `_template.html` (في جذر المشروع) كنقطة بداية لأي صفحة جديدة — الكود موجود فيه بالفعل.

للتحقق السريع إن كل الصفحات فيها الكود:
```bash
find . -name "*.html" | xargs grep -L "adsbygoogle.js"
```
لو الأمر ده رجّع أي أسماء ملفات، يبقى الملفات دي ناقصة الكود ولازم تتضاف يدويًا.
