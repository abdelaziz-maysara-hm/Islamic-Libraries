# Roadmap — المكتبة الجامعة

هدف المشروع: بناء أكبر منصة عربية ثابتة (static) للمحتوى الإسلامي الموثّق، مع بقاء الزائر داخل الموقع قدر الإمكان.

## الأهداف الكمّية

| المجال | الوضع الحالي (تقريبي) | الهدف |
|--------|------------------------|--------|
| كتب | ~200 كتاب في `ALL_BOOKS` | **5000+ كتاب** |
| فتاوى | مئات الفتاوى عبر ملفات `fatawa-*.js` | **10000+ فتوى** |
| فيديوهات | صفحات أقسام + روابط قنوات | **5000+ درس/فيديو** |
| أدوات | 3 أدوات أساسية | توسيع تدريجي (مواقيت، قبلة، أذكار تفاعلية...) |
| قاموس / مراجع | صفحات أولية | تغطية أوسع للمصطلحات والمراجع |

## مبادئ ثابتة (لا تُكسر)

1. **Static + Vanilla JS فقط** — بدون framework أو build إجباري للواجهة.
2. **عدم تغيير المسارات العامة** — الروابط المفهرسة تبقى كما هي.
3. **إبقاء الزائر داخل الموقع** — صفحة تفصيل داخلية أولاً، والرابط الخارجي (شاملة / يوتيوب) ثانوي.
4. **مصادر موثّقة** — كل كتاب/فتوى بمصدر واضح؛ لا محتوى بلا مرجع.
5. **التحقق** — `npm run validate` قبل أي commit مؤثر على الصفحات العامة.

## مراحل العمل

### المرحلة A — استقرار المنتج (قيد التنفيذ / قريب)
- [x] إصلاح `books-all.html` (كانت placeholder) + بحث + فلاتر + pagination
- [x] إصلاح UX قائمة الفتاوى (dropdown) وقائمة الموبايل
- [x] توحيد AdSense publisher / ads.txt (57/57 صفحة موحّدة على `ca-pub-9572691438076734`)
- [ ] تدقيق كل روابط `shamela.ws` والروابط الخارجية -- **قيد التنفيذ**: 18/175 كتابًا في الملف
  الأساسي مفحوصة (8 أخطاء مُصححة، نسبة خطأ ~44%)، بالإضافة لاكتشاف تكرارين (كتاب واحد مُدخل
  مرتين بعنوانين) يحتاجان قرار دمج من صاحب الموقع. 157 كتابًا متبقية + كتب `books-extra.js`
  الـ29 لم تُراجَع بعد. انظر تفاصيل كل جولة في `CHANGELOG.md`.
- [ ] توحيد معرّف AdSense في بقية صفحات HTML القديمة
- [x] تحسين صفحة `book.html` جزئيًا: قسم "كتب أخرى لنفس المؤلف" (بيانات موجودة، بدون محتوى مولّد).
  الملخصات النصية للكتب مؤجَّلة عمدًا -- تخالف `AI_RULES.md` ("لا تولّد محتوى ديني غير موثّق")
  ما لم تُكتب بمصدر حقيقي موثّق لكل كتاب على حدة
- [ ] نفس الفكرة للفتاوى: نص الفتوى كامل على الموقع + مصدر

### المرحلة B — توسيع الكتب إلى 1000 ثم 5000
- [ ] توحيد مصدر البيانات في `assets/js/books-data.js` + `books-extra*.js` (أو تقسيم حسب التصنيف)
- [ ] حقول اختيارية لكل كتاب: `summary`, `pages`, `topics[]`, `verified`
- [ ] استيراد دفعات موثّقة (شاملة / أرشيف) مع مراجعة يدوية للروابط
- [ ] صفحات أقسام الكتب تعكس نفس البيانات (تقليل التكرار اليدوي في HTML)
- [ ] عدّاد حي على الواجهة من `ALL_BOOKS.length` بدل أرقام ثابتة

### المرحلة C — توسيع الفتاوى إلى 10000
- [ ] الإبقاء على نمط الملفات المقسّمة (`fatawa-expand-*.js`, `fatawa-extra*.js`) لتفادي ملفات ضخمة
- [ ] schema موحّد: `{ id, q, a, cat, source, tags[] }`
- [ ] بحث فتاوى أسرع + pagination داخل `fatawa.html`
- [ ] ربط كل قسم (`sec-*`) بعدد حي

### المرحلة D — الفيديوهات إلى 5000
- [ ] كتالوج `VIDEOS_DATA` مشابه للكتب (id, title, sheikh, section, youtubeId أو url)
- [ ] صفحات أقسام تعتمد على البيانات + pagination
- [ ] تضمين YouTube embed داخل الموقع عند توفر المعرّف (البقاء في الصفحة)

### المرحلة E — جودة واكتشاف
- [ ] تحديث `sitemap.xml` تلقائياً أو بسكربت عند إضافة صفحات
- [ ] توسيع `SITE_INDEX` مع كل دفعة محتوى
- [ ] مراقبة Lighthouse / validate في CI

## أين تُضاف البيانات؟

| نوع | الملفات الأساسية |
|-----|------------------|
| كتب | `assets/js/books-data.js` (base عبر CDN pin) + `assets/js/books-extra.js` |
| فتاوى | `assets/js/fatawa-expand-old*.js`, `fatawa-extra*.js` |
| بحث عام | `assets/js/search-data.js` |
| مواضيع سريعة | `assets/js/topics.js` |
| صفحات HTML للأقسام | `books-*.html`, `fatawa/*.html`, `videos*.html` |

## سياسة المعاينة داخل الموقع (Preview)

- **كتاب:** المستخدم يفتح `book.html?book=ID` ويرى العنوان، المؤلف، التصنيف، ملخصاً إن وُجد، زر مفضلة، ثم رابط الشاملة.
- **فتوى:** النص يظهر في accordion/صفحة داخلية؛ المصدر للرجوع لا للخروج الإجباري.
- **فيديو:** embed إن أمكن؛ وإلا بطاقة داخلية + رابط القناة.

## مساهمة لاحقة

1. اقرأ `AGENTS.md` و `AI_RULES.md`.
2. حدّث هذا الـ Roadmap عند إغلاق بند أو فتح مرحلة.
3. سجّل التغيير في `CHANGELOG.md`.
4. شغّل `npm run validate`.
